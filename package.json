{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "le portfolio de lionel ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon App.js",
    "sass": "sass --watch public/styles/sass/index.scss public/styles/sass/index.css",
    "start": "concurrently \"npm run server\" \"npm run sass\"",
    "author": "Lionel Mambingo",
    "license": "ISC",
    "dependencies": {
      "express": "^4.18.2",
      "nodemon": "^2.0.22",
      "sass": "^1.49.11",
      "sass-watch": "^0.1.0",
      "concurrently": "^8.2.2"

    }
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
